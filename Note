to install docker 

sudo apt-get update
sudo apt-get upgrade
sudo apt-get install -y docker.io

---kubectk
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl


create docker group to solve persmission issues
sudo groupadd docker
sudo usermod -aG docker $USER

to re-evaluatedyour group memebership
su -s ${USER} you need password

//k3d
wget -q -O - https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash

k3d cluster create dev-cluster --port 80:80@loadbalancer --port 443:443@loadbalancer --port 8080:8080@loadbalancer
Map ports from the node containers (via the serverlb) to the host 
(Format: [HOST:][HOSTPORT:]CONTAINERPORT[/PROTOCOL][@NODEFILTER])
- Example: `k3d cluster create --agents 2 -p 8080:80@agent:0 -p 8081@agent:1`

kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

creat name space in manifest file or - n flag

k3d cluster create dev-cluster --k3s-arg "--disable=traefik@server:0" --port 80:80@loadbalancer --port 443:443@loadbalancer --port 8080:8080@loadbalancer

k3d cluster create dev-cluster  --port 80:80@loadbalancer --port 443:443@loadbalancer --port 8080:8080@loadbalancer

initial_root_password

sudo kubectl cp gitlab.rb gitlab-deployment-846b8ff458-vjv2n:etc/gitlab/gitlab.rb   -n gitlab
sudo kubectl cp gitlab.rb gitlab-deployment-846b8ff458-vjv2n:etc/gitlab/gitlab.rb   -n gitlab